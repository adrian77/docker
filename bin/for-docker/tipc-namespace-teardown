#!/bin/bash
LAN=intra
NS=$1

# Delete a namespace
# arg1: namespace name
function __delete_namespace()
{
    local ns=$1
    ip netns exec $ns ip link set $LAN down > /dev/null
    ip netns del $ns > /dev/null 
}

__delete_namespace $NS
